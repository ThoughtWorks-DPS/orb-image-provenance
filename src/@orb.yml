# yamllint disable rule:line-length
---
version: 2.1

description: |
  Commands for signing, sbom generation, and uploading of provenance content to registry.

display:
  source_url: "https://github.com/ThoughtWorks-DPS/orb-image-provenance"
